type: bulk
args:

- type: run_sql
  args:
    sql: |
      CREATE TABLE IF NOT EXISTS fork_ws_items (
        id SERIAL PRIMARY KEY,
        name TEXT NOT NULL
      );
      INSERT INTO fork_ws_items (id, name) VALUES (1, 'Item 1') ON CONFLICT DO NOTHING;

- type: track_table
  args:
    schema: public
    name: fork_ws_items

- type: create_select_permission
  args:
    table: fork_ws_items
    role: user
    permission:
      columns: '*'
      filter: {}
